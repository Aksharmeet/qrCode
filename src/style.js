import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  app: {
    padding: "20px",
    height: "90vh",
    display: "flex",
    alignItems: "center",
    background: "#9fb7cd",
  },
}));
