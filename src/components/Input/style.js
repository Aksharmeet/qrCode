import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  inputBlock: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    height: "",
    padding: "10px",
    background: "#e7edf2",
  },
  input: {
    width: "100%",
    height: "40px",
  },
}));
