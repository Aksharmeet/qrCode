import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  SvgContainer: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    height: "200px",
    padding: "10px",
    background: "#e7edf2",
  },
}));
